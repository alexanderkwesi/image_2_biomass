# Check Node.js version
node --version

# Check npm version
npm --version



# Install Expo CLI globally
npm install -g expo-cli

# Verify installation
expo --version


# Install the new Expo CLI tools
npm install -g expo

# Also install eas-cli for Expo Application Services
npm install -g eas-cli

# Verify both
expo --version
eas --version


##do not do if project is active
# Create new Expo project
npx create-expo-app MyExpoApp

# Navigate to project
cd MyExpoApp

# Install dependencies
npx expo install



# Navigate to your project folder
cd C:\Users\user\Documents\image_2_biomass_ml_project_version_1

# Install all dependencies from package.json
npm install

# Install Expo-specific packages
npx expo install react-native react-native-web react-native-safe-area-context

# Install navigation (common)
npx expo install @react-navigation/native @react-navigation/stack
npx expo install react-native-screens react-native-safe-area-context
npx expo install react-native-gesture-handler


##if above does not work try this next after steps above
# Uninstall current React
npm uninstall react react-dom

# Install React 18
npm install react@18.2.0 react-dom@18.2.0

# Or use Expo's recommended versions
npx expo install react@18.2.0 react-dom@18.2.0



##if the above does not work try
# Check if your Expo version supports React 19
# First, check Expo SDK version in app.json/expo.json
cat app.json

# If you have an older Expo SDK, you might need to upgrade
# Install React 19
npx expo install react@latest react-dom@latest

## or the below or cat app.json or expo.json then a single of the other code
cat expo.json

# If you have an older Expo SDK, you might need to upgrade
# Install React 19
npx expo install react@latest react-dom@latest

## if the above does not work then

##run

.\clean_up_project.json

## if above does not work use yhe PowerShell Script 

.\create_a_new_package.json.ps1


## then install for this project

npm i expo 

## without react-native-windows-camera using the below


.\updated_package.json_without_bad_dependencies.ps1

##maintain the below in for package.json


  "workspaces": [
    "mobile-frontend",
    "backend-services"
  ],
  "scripts": {
    "start": "expo start",
    "reset-project": "node ./scripts/reset-project.js",
    "android": "expo start --android",
    "serve:android": "npx react-native run-android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "lint": "expo lint",
    "python-build": "cd backend-services &&  python ../backend-services/database_connection/db_connection.py  && python ../backend-services/api-gateway/app/main.py",
    "start-api": "cd backend-services/api-gateway/app && python -m uvicorn main:app --reload --host 0.0.0.0 --port 5000",
    "dev": "npm run start-api",
    "serve": "npx react-native start --host 0.0.0.0",
    "all": "concurrently \"npm run web\" \"npm run python-build\"  \" npm run dev\"  \"npm run serve\"    \"npm run serve:android\"     "
  },






########if everything else fails delete the expo folder and reinstall with the below


# OPTION 1: Standard install with --no-optional (RECOMMENDED)
npm install --no-optional --legacy-peer-deps

# OPTION 2: Force install ignoring optional deps
npm install --omit=optional --legacy-peer-deps

# OPTION 3: Using force flag if needed
npm install --no-optional --legacy-peer-deps --force




########## or create an expo project


# Create new Expo project (recommended)
npx create-expo-app image_2_biomass_project_version_1

# With TypeScript template
npx create-expo-app MyExpoApp --template expo-template-blank-typescript

# With navigation template
npx create-expo-app MyExpoApp --template tabs

# Navigate into project
cd \Users\user\Documents\image_2_biomass_ml_project_version_1





################# then 


# Make sure you're in your project directory
pwd  # Should show your project folder

# Install Expo
npm install expo@^51.0.0 --legacy-peer-deps

# Verify installation
npm list expo --depth=0




npm install -g react-native-cli

# 对于 Android
npx react-native run-android

# 对于 iOS
npx react-native run-ios